import React from "react";
import Layout from "../components/Layout.jsx";  // Corrected the import path if Layout is inside the "components" folder

const HomePage = () => {
  return (
    <Layout>
      <h1>Home Page</h1>
    </Layout>
  );
};

export default HomePage;